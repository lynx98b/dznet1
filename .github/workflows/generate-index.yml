name: Génération index_global.json
on:
  push:
    branches: ["main"]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - run: pip install beautifulsoup4

      - name: Générer index
        run: python generate_index_global.py

      - name: Commit & push
        run: |
          git config user.name "github-actions"
          git config user.email "actions@github.com"
          git add index_global.json
          git commit -m "🔄 update index_global.json" || echo "No changes"
          git push || echo "No push"
