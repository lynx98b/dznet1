name: Génération automatique index_global.json
on:
  push:
    branches: ["main"]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - uses: actions/setup-python@v4
        with:
          python-version: 3.11

      - run: pip install beautifulsoup4

      - run: python generate_index_global.py

      - run: |
          git config user.name "github-actions"
          git config user.email "actions@github.com"
          git add index_global.json
          git commit -m "🔄 Mise à jour automatique index_global.json" || true
          git push || true
